/// The file format for exporting scanned mesh data.
enum ExportFormat {
  /// Wavefront OBJ — a widely supported text-based 3D format.
  obj,

  /// Binary glTF (GLB) — a compact binary format for 3D applications.
  glb,
}
