/// The scanning mode to use for a session.
enum ScanMode {
  /// Use the rear LiDAR sensor for environment scanning.
  lidar,

  /// Use the front-facing TrueDepth camera for face/depth scanning.
  trueDepth,
}
